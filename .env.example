# docker compose 용 환경변수 — 이 파일을 .env 로 복사해서 값을 채운다 (.env 는 git 에 안 올라감)
ADMIN_PASSWORD=바꿔서-쓰세요
# 호스트에서 열 포트 (컨테이너 안은 항상 3000)
HOST_PORT=3000
# HTTPS 프록시 뒤에 둘 때만 1
COOKIE_SECURE=
# debug 면 대전 answer/tick 상세 로그
LOG_LEVEL=
# 컨테이너 시간대 (로그·백업 폴더 이름)
TZ=Asia/Seoul
